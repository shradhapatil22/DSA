#include<iostream>
#include<set>
using namespace std;
int main(int argc, char** argv)
{
    string str;
    cin >> str;
    set<char> set;
    int max=0;
    int s=0;
    int e=0;

    while (s<str.size())
    {
        auto it=set.find(str[s]);
        
    }
    
    return 0;
}